# 后续计划

当前版本的基础功能、诊断、测试、性能优化和源码发布流程已经完成。这里只保留仍需处理
或明确暂缓的事项，不记录已完成历史。

## TODO

- 多输出逐实例剔除

在 `renderGpu()` 中按保守包围盒跳过完全位于其他输出之外的 Trail、点击实例和 Ring4
粒子，优先评估多屏场景的 CPU 几何生成收益。实现时必须保持 `inst.drawn = true` 的年龄
推进语义，并验证跨屏边界、damage 和 Bloom 外扩。

- 桌面混合开关：后续可以增加的简化路径。
- 应用白名单/黑名单
- 多点触控支持

## 暂缓或不做

- GPU 资源预热：首次点击初始化有卡顿风险，但主动制造 repaint 的收益和副作用
  需要实际嵌套会话数据后再评估。
- 广色域 BT.709/容器基色矩阵：需要真实广色域设备和 KWin 色彩矩阵方向验证。
- Unity `Maximum Allowed Timestep` 对齐：当前 0.1 秒上限优先保证 KWin 卡顿后的稳定性。
- Bloom 最小尺寸改为 1：当前 2 像素下限避免极窄输出上的退化采样。
- `TriBurstInstance` 等 Ring4 类型迁移到其他头文件：仅改善归属，不带来运行时收益。
- 洋红色 DebugDamage 区域及更复杂的 damage 可视化：暂不改变现有调试显示语义。
- 点击实例数量上限：可能改变极端高频点击时的可见行为，保持 Unity 等价优先。
- draw call 合并：需要拆分生成与提交阶段并做逐帧视觉回归，当前收益不足以抵消风险。
- OBJECT library 构建重构：只减少重复编译，不改善运行时性能，暂不改变 KWin/AutoMoc
  链接结构。
- 截图或逐像素回归测试：结果受 GPU、驱动、色彩管理和 KWin 输出路径影响，继续使用
  逻辑测试与嵌套会话人工验证。
- 低分辨率 Bloom、持久映射 VBO、几何缓存和 Shader 缓存：现有性能数据没有显示足够
  收益。
- 更多用户可调参数：当前默认值以 Unity 等价和稳定性优先。
- 跨发行版二进制打包：继续发布源码包和 SHA-256 校验文件。
