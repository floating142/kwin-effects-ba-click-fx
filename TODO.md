# kwin-effects-ba-click-fx 后续 TODO

本文档记录 1.0 版本之后可以实现的功能和工程改进。除非特别注明，所有新增
自定义选项都应以 Unity 等价值为默认值，并保持关闭时的现有视觉结果不变。

## 近期可实现

- [ ] 增加统一的诊断命令，自动执行 status、log、日志分类查询和插件路径检查。
- [ ] 在 KCM 中提供“复制诊断信息”按钮，输出 KWin 版本、插件 build id、GPU 信息、
      当前输出尺寸、缩放比例和资源路径。
- [ ] 增加运行时日志级别：关闭、错误、实例、帧统计、详细调试。
- [ ] 为帧统计增加固定格式摘要，区分 CPU、GPU、重绘区域、矩形数量和跳帧原因。
- [ ] 增加锁屏、全屏窗口和输出切换时的自动清理测试。
- [ ] 增加正式的跨显示器、HiDPI、旋转输出和 HDR 输出测试记录。
- [ ] 在readme中增加preview

## 输入与拖尾

- [ ] 提供输入采样策略：每事件采样、按时间间隔采样、按距离采样和自适应采样。
- [ ] 提供最小采样距离、最大跳跃距离、最大采样点数和速度平滑系数配置。
- [ ] 增加输入事件合并统计，记录鼠标事件频率、丢弃样本数和跨屏跳跃次数。
- [ ] 为高回报率鼠标增加采样预算，避免事件过密时 CPU 几何生成占用过高。
- [ ] 为拖尾平滑提供 Unity 等价模式和低延迟模式。

## 视觉自定义

- [ ] 可选的整体颜色覆盖，默认使用 Unity 原始颜色。
- [ ] 可选的拖尾宽度、长度、核心亮度和辉光强度，默认值保持 Unity 参数。
- [ ] 可选的 Ring、MeshTri、Ring3、Ring4 和拖尾图层开关。
- [ ] 可选的距离发射间距、粒子数量上限和粒子整体缩放。
- [ ] 提供预设管理：Unity 默认、低亮度、低负载和用户自定义预设。
- [ ] 为自定义参数增加“恢复 Unity 默认值”操作，并显示当前值是否偏离默认值。

## 性能与渲染

- [ ] 评估持久映射 VBO、批次容量复用和几何缓存，减少高频拖动时的 CPU 分配。
- [ ] 根据 GPU 计时和重绘面积自动选择辉光更新策略，同时保持 Unity 等价模式可用。
- [ ] 增加逐输出资源状态诊断，显示 HDR 场景、背景纹理和辉光金字塔的尺寸。
- [ ] 为着色器资源增加版本校验，避免插件二进制与旧 shader 文件混用。
- [ ] 评估 KWin/Qt 着色器缓存接口，减少重启或热重载时的编译等待。
- [ ] 在不改变默认效果的前提下，增加低分辨率辉光或关闭辉光的性能预设。

## 稳定性与兼容性

- [ ] 增加资源缺失、着色器语法错误和 OpenGL 能力不足时的可读错误提示。
- [ ] 增加安装后资源完整性检查，覆盖插件、配置模块、贴图、网格和 shader 文件。
- [ ] 增加卸载脚本以及旧版本迁移的回滚提示。
- [ ] 增加不同 KWin ABI、Wayland 嵌套会话和系统级安装路径的自动检查。
- [ ] 记录已知 GPU 驱动差异，特别是 HDR 传递函数、GLSL 1.40 和多输出场景。

## 发布工具

- [ ] 增加 verify-release.sh，检查版本号、许可证、资源清单、插件加载状态和 build id。
- [ ] 增加源码压缩包、SHA-256 校验文件和变更日志生成脚本。
- [ ] 增加干净目录安装/卸载测试，避免构建目录或旧用户资源影响结果。
- [ ] 发布 KDE Store 所需的截图、GIF、兼容性说明和故障排查文档。
