# 后续计划

当前版本已经完成基础功能、诊断、自动测试、性能优化和轻量发布流程。后续事项按
“值得做”与“暂不做”区分，避免把已经完成的实现细节继续当作待办事项。

## 值得做

### 1. 完善 damage 可视化

- [ ] 重新设计 `deviceRegion` 调试显示，区分插件申请区域、KWin 实际重绘区域和最终
      输出裁剪区域。
- [ ] 确保调试线框本身不会参与下一帧 damage，避免可视化反馈回路。

完成条件：打开调试显示时不会持续制造额外 damage，且三类区域可以直接区分。

### 2. 评估精确桌面混合开关

- [ ] 在已有 direct texture / blit 两条路径上评估“精确桌面混合”选项。
- [ ] 默认保持当前精确路径；简化路径只有在确认没有黑块、旧帧残留和输出切换残留后
      才考虑加入。

完成条件：开关不改变默认视觉结果，并有普通输出、HiDPI、双输出、旋转和 HDR 验证记录。

### 3. 持续维护兼容性验证

- [ ] KWin 新版本发布后重新运行 ABI、嵌套会话和安装校验。
- [ ] 补充新的 GPU/驱动组合时，只记录可复现的差异和必要的规避方案。

完成条件：升级 KWin 后能明确判断是 ABI 不匹配、资源问题还是运行时渲染问题。

## 暂不做

- 不加入截图或逐像素回归测试；当前效果受 GPU、色彩管理和 KWin 输出路径影响，继续
  使用逻辑测试与嵌套会话人工验证。
- 不增加低分辨率 Bloom、持久映射 VBO、几何缓存或 Shader 缓存；现有性能数据没有显示
  足够收益。
- 不增加更多用户可调参数；当前默认值以 Unity 等价和稳定性优先。
- 不扩展跨发行版二进制打包；当前保留源码包、SHA-256 和基础 CI 即可。

## 已完成概览

- 运行时诊断、GPU/输出状态、输入统计和固定格式帧统计。
- 曲线、拖尾、距离发射、粒子上限和 damage 逻辑测试。
- 多输出、HiDPI、旋转、HDR、锁屏、全屏窗口和嵌套会话验证记录。
- Trail/Ring4 路径重采样、damage 传播、背景 direct texture / blit fallback 和临时容器复用。
- `verify-release.sh`、`make-release.sh`、SHA-256 产物和 GitHub Actions 基础 CI。
- 用户级/系统级安装、卸载、ABI 检查、配置热重载和失败回滚提示。
