# kwin-effects-ba-click-fx 实现路线

本文档按实现依赖和风险排序。每个阶段应在默认配置下保持 Unity 等价视觉结果，
新增自定义选项也应以 Unity 等价值为默认值。

## 阶段 0：诊断基础（已完成）

- [x] 增加统一的运行时诊断接口，输出插件、GPU、显示输出和资源状态。
- [x] 在诊断信息中输出与插件 ABI 对应的精确 KWin 版本。
- [x] 在 KCM 中提供复制诊断信息、生成诊断报告和打开诊断目录操作。
- [x] 为帧统计增加固定格式摘要，区分 CPU、GPU、重绘区域和矩形数量。
- [x] 在 README 中增加 preview。
- [x] 增加卸载脚本。
- [x] 增加基础 verify-release.sh，检查版本号、许可证、源码资源和构建接线。

## 阶段 1：日志与可观测性

- [x] 将日志开关改为运行时级别：关闭、错误、实例、帧统计和详细调试。
- [x] 为资源缺失、Shader 编译失败和 OpenGL 能力不足提供固定格式错误信息。
- [x] 增加输入统计字段：事件频率、接受样本数、合并数、丢弃数和跨屏跳跃数。
- [x] 增加可归因的跳帧原因统计。
- [x] 完善逐输出资源状态诊断，明确 HDR Scene、背景纹理和各级 Bloom 尺寸。

完成条件：关闭日志时不启用 GPU timer；各日志级别不会输出更高等级的信息；诊断报告
可以区分配置、资源、输入、CPU、GPU 和输出状态。

## 阶段 2：稳定性与自动测试

- [x] 为曲线求值、TrailStream 和距离发射间距增加单元测试。
- [x] 为粒子上限增加单元测试。
- [x] 为 damage 网格量化计算增加单元测试。
- [x] 暂不加入截图/像素回归测试；当前以纯逻辑测试和嵌套会话人工验证为准。
- [x] 增加锁屏、全屏窗口和输出切换时的嵌套会话清理验证清单。
- [x] 增加跨显示器、HiDPI、旋转输出和 HDR 输出测试记录模板。
- [x] 增加安装后资源完整性检查，覆盖插件、KCM、贴图、网格和 Shader。
- [x] 增加 KWin ABI、Wayland 嵌套会话和系统级安装路径检查。
- [x] 记录已知 GPU 驱动差异，重点覆盖 HDR、GLSL 1.40 和多输出场景。
- [x] 增加旧版本迁移和失败回滚提示。

完成条件：后续输入和渲染改动可以由自动测试发现生命周期、坐标、资源和 damage 回归。

## 阶段 3：输入采样与拖尾

- [x] 抽离路径距离重采样逻辑，使 Trail 和 Ring4 共用可测试的线段遍历基础。
- [x] 验证低频长事件与高频短事件产生相同的拖尾点和 Ring4 空间分布。
- [x] 区分 pointerMotion 事件、mouseChanged 事件、帧级回退采样和零距离采样统计。
- [x] 异常单次移动暂不增加跳跃距离或补点上限，继续保留累计距离语义。
- [x] 根据实际性能数据暂不增加采样预算和用户配置。

完成条件：默认策略与当前行为一致；采样预算不会改变 Ring4 的累计路程语义；统计能够
解释每个被合并或丢弃的样本。

## 阶段 4：拖尾行为与性能选项

- [x] 增加“无需按住鼠标”选项，普通光标移动时也能生成拖尾。
- [x] 将沿途小三角设为拖尾子功能；关闭拖尾时同步停用 Ribbon 和 Ring4。
- [ ] 增加“精确桌面混合”选项，默认开启；关闭时使用简化背景合成路径。

完成条件：默认配置保持当前视觉和交互；持续拖尾只在光标移动时产生内容；关闭拖尾
必然关闭沿途小三角；简化背景混合不会产生黑块、旧帧残留或输出切换残留。

## 阶段 5：性能与渲染策略

- [x] Bloom 固定使用 Unity PPv2 等价参数和完整金字塔，不增加低分辨率、关闭或自适应模式。
- [x] 记录持续拖尾性能基线，确认输入、CPU 几何生成和 GPU 渲染当前无明显瓶颈。
- [x] 增加逐输出 GPU 分段、像素面积和 Region 数量统计；CPU 分段保持全局汇总。
- [x] 按输出计算 Bloom damage，避免使用全局最大半径扩展所有输出。
- [x] Bloom 已使用逐级 Region 传播替代理论最大半径更新整条金字塔。
- [x] 评估相邻 Region 合并：当前每级已合并为一次 VBO 提交；Import 的 blit 仍需逐矩形执行，暂不扩大像素面积。
- [ ] 重新设计完整 `deviceRegion` 可视化，避免诊断线自身参与 damage 并形成反馈；当前仅显示插件申请区域。
- [x] 检查高频路径中的临时容器分配，复用拖尾笔划、法线、方向和顶点容器容量。
- [x] 用相同双输出持续拖动场景完成前后对比：拖尾 CPU 阶段约下降 35%，总 CPU 约下降 17～20%。
- [x] 暂不引入持久映射 VBO、几何缓存或 Shader 缓存；当前数据没有显示足够收益。

完成条件：Bloom 和最终画面保持 Unity 等价；优化不改变采样、几何、damage 或图层顺序；
没有测量收益的复杂缓存不进入实现。

## 阶段 6：发布自动化

- [ ] 扩展 verify-release.sh，检查已安装插件、运行状态和 build id。
- [ ] 增加干净目录的安装、诊断和卸载测试。
- [ ] 增加源码压缩包、SHA-256 和变更日志生成脚本。
- [ ] 准备 KDE Store 截图、GIF、兼容性说明和故障排查文档。

完成条件：发布产物可以从干净源码目录一次生成，并能验证安装文件、运行二进制与源码
build id 一致。
